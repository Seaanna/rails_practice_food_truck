<%= form_with(model: truck_category, local: true) do |form| %>
  <% if truck_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(truck_category.errors.count, "error") %> prohibited this truck_category from being saved:</h2>

      <ul>
      <% truck_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :truck_id %>
    <%= form.text_field :truck_id, id: :truck_category_truck_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :truck_category_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
